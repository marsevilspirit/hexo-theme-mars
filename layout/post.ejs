<div class="post">
    <% if (is_post()) { %>
        <h1><%= page.title %></h1>
    <% } %>

    <main class="post-md">
        <%- page.content %>
    </main>

    <p><%- time(page.date, 'MMM YYYY') %></p>
</div>
